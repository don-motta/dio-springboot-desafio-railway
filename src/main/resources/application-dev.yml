spring:
  datasource:
    url: jdbc:h2:mem:dio2025
    username: admin
    password:
  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        trace: false
        web-allow-others: false

# incluir este arquivo na configuração do Environment Variables: SPRING_PROFILES_ACTIVE=dev